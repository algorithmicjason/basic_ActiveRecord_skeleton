require_relative './config/environment'
require 'sinatra/activerecord/rake'

desc "runs a pry"
task :console do
    Pry.start
end

#use rake -T to see all the task options
#use rack to write functions in ruby & your app can incorporate it